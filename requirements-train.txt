# Mavis — dependencies for retraining the pose models.
# Only needed if you are running anything under tools/ or *-lstm.py.
#
#     pip install -r requirements-train.txt
#
# macOS / Apple Silicon: do NOT also install `tensorflow-macos`. TensorFlow
# 2.16+ ships native arm64 wheels under the plain `tensorflow` name, and having
# both installed produces a broken, half-shadowed package.

-r requirements.txt

tensorflow==2.16.1
keras==3.12.0
scikit-learn==1.7.2
pandas==2.3.3
