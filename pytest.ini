[pytest]
# Repo root on sys.path so tests can `import analyzer` / `import exercises`
# without an editable install.
pythonpath = .
testpaths = tests
filterwarnings =
    ignore::UserWarning:google.protobuf.symbol_database
